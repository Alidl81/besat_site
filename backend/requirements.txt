Django>=5.2,<6.1
djangorestframework>=3.15,<4.0
django-filter>=24.3,<26.0
drf-spectacular>=0.27,<0.29
django-cors-headers>=4.4,<5.0
django-environ>=0.11,<1.0
psycopg[binary]>=3.2,<4.0
Pillow>=12.3.0,<13.0
whitenoise>=6.7,<7.0
gunicorn>=22.0,<24.0
djangorestframework-simplejwt>=5.3,<6.0
nh3>=0.2,<1.0
django-prometheus>=2.3,<3.0

# Transitive-dependency CVE floors (not direct requirements of this
# project -- pulled in by the base image / other packages above). Pinned
# explicitly here because a fresh `pip install -r requirements.txt` would
# otherwise resolve an older, vulnerable version. See
# docs/security/DEPENDENCY_SECURITY.md for the Trivy scan that found these.
msgpack>=1.2.1  # CVE (GHSA-6v7p-g79w-8964)
setuptools>=78.1.1  # CVE-2025-47273 (path traversal)