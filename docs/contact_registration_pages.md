# Contact and Registration Pages

## Frontend routes

```txt
/contact
/registration
Backend routes needed
GET /api/contact/
GET /api/registration/
Rules
اطلاعات تماس فقط زمانی نمایش داده شود که در سامانه ثبت شده باشد.
اطلاعات ثبت‌نام فقط زمانی نمایش داده شود که در سامانه ثبت شده باشد.
تا قبل از ثبت اطلاعات، صفحه باید حالت خالی نمایش دهد.

