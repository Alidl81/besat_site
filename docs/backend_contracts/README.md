
Backend Contracts

این پوشه قراردادهای لازم برای پیاده‌سازی بک‌اند بر اساس فرانت‌اند نهایی پروژه بعثت را نگه می‌دارد.

هر بار که یک قابلیت در فرانت ساخته یا تغییر می‌کند، قرارداد مرتبط با بک‌اند باید اینجا ثبت یا به‌روزرسانی شود.

فایل‌های اصلی
docs/backend_contracts/current_frontend_api_contract.md
docs/backend_contracts/feature_contract_template.md
اصل پروژه

فرانت‌اند منبع اصلی نیازمندی‌های محصول است. بک‌اند باید مطابق routeها، فرم‌ها، roleها و رفتارهای نهایی فرانت پیاده‌سازی شود.
