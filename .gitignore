# Local backups
_backups/
deploy/production.compose.env
deploy/backups/

# Scratch/reference workspace (venv, npm cache, stale snapshots) --
# never meant to be version-controlled; was tracked by accident.
work/

# Node dependency trees (each app has its own; this guards against an
# accidental root-level install, e.g. from running a tool in the wrong dir)
node_modules/

# Python cache
__pycache__/
*.py[cod]
*$py.class

# Django / Python cache inside backend
backend/**/__pycache__/
backend/**/*.py[cod]

# Virtual environment
backend/venv/
backend/env/
backend/.venv/
venv/
env/
.venv/

# Environment files
backend/.env
backend/.env.*
!backend/.env.example
.env
.env.*
!.env.example
docker.env
!docker.env.example

# Local agent orchestration scaffolding (worktrees for parallel sub-agents)
.claude/

# SQLite database
backend/db.sqlite3
backend/db.sqlite3-journal
*.sqlite3

# Logs
*.log

# Django collected static / uploaded media
backend/staticfiles/
backend/media/

# Test / lint / type-check cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE / Editor
.vscode/
.idea/
*.swp

# OS files
.DS_Store
Thumbs.db
frontend/.env.local
frontend/.env*.local


# Local agent skills
.agents/

# Local audit evidence and exported audit bundle (never source-controlled).
.audit/
.audit.zip

# Generated browser/coverage traces and reports.
*.trace
*.har
coverage/
.nyc_output/
lighthouse*.html

# OPS-ACCIDENTAL-QA-ARTIFACTS-001: generated QA/dev-testing trees at the
# repo root, produced by the frontend QA pipeline's own runtime tooling --
# not meant to be version-controlled (some contain dev-only seed
# credentials, route traces, and screenshot data that must never be
# committed, even accidentally via a broad `git add`/archive).
.qg_runtime_tests/
frontend-artifacts/

# Stray empty directories from a one-off Windows-path shell-quoting issue
# in QA tooling (a literal Windows path/semicolon ended up as a directory
# name on this Linux checkout). Harmless (empty) but must never be tracked.
# Backslashes are gitignore escape characters, not literal path separators,
# so each one here must itself be escaped to match the literal directory
# name this filesystem actually has.
C:\\Users\\Ali\\AppData\\Local\\Temp\\besat-qg-qg-20260827/
frontend;D/
