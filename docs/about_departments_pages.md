# About and Departments Pages

## Frontend routes

```txt
/about
/departments
Backend routes needed
GET /api/about/
GET /api/departments/
Rules
معرفی مدرسه فقط زمانی نمایش داده شود که در سامانه ثبت شده باشد.
دپارتمان‌ها فقط زمانی نمایش داده شوند که در سامانه ثبت شده باشند.
تا قبل از ثبت اطلاعات، صفحه باید حالت خالی نمایش دهد.

